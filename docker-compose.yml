---
version: "3.9"

services:
    tfc:
        container_name: bootstrap-icons-example-app
        image: bootstrap-icons-example-app
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        ports:
            - "8080:8811"
